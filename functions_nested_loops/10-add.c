#include "main.h"

/**
 * add - adds two integers and returns the result
 * @l: First integer
 * @m: Second integer
 *
 * Return: n = l + m
 */
int add(int l, int m)
{
	int n = (l + m);

	return (n);
}
